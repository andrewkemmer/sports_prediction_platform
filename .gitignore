# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/
.venv/
venv/

# Environment / secrets
.env
.env.*

# Model artifacts (never commit trained binaries)
*.joblib
*.pkl

# Notebooks checkpoints
.ipynb_checkpoints/

# OS
.DS_Store

# Raw data caches (DuckDB/Parquet stores) — never committed
store/
data_delivery/

# Agent session scratch (local logs only) — never committed
.freebuff/

# Operational run evidence (regenerated per run; never committed)
ops/probes/
ops/_probe_cache/
ops/*.log
