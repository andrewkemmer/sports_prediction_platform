"""Sync package (Phase r1 §2 restructure).

Manual, contract-only GitHub sync (§2.1 scope): lands in Phase r8 with
the experiments CLI. This package exists now so the §2 tree is complete;
it never runs inside a production path.
"""
